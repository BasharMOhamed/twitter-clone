const express = require("express");

const router = express.Router();

// login
router.post("sign-in");

// sign-up
router.post("");

// logout
router.get("");

// check auth
router.get("");

module.exports = router;
